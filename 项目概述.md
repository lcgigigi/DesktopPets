# 华力 AI 桌面助手项目概述

## 1. 项目定位

这是一个基于 **Tauri 2 + Vue 3** 开发的跨平台桌面助手。

它以可拖拽的桌面吉祥物形式常驻桌面，作为华力 Web 工作台的快捷入口，同时负责接收任务和站内消息提醒。

项目本身不是完整的待办管理系统，主要承担以下职责：

- 桌面快捷交互
- Web 平台登录联动
- 一句话待办入口
- 实时消息接收
- 桌面提醒展示
- Web 工作台和日历页面跳转

## 2. 当前主要功能

### 2.1 桌面吉祥物

- 透明无边框窗口
- 始终置顶
- 支持跨工作区显示
- 支持鼠标拖拽
- 点击吉祥物打开或关闭输入面板
- 根据状态切换问候、工作等动画
- 收到消息时显示气泡或通知卡片

### 2.2 一句话创建入口

用户可以在面板中输入：

```text
明天下午三点提醒我确认会议纪要
```

当前实现会携带输入内容打开 Web 工作台：

```text
/workbench?from=desktop&desktopTodoText=输入内容
```

实际的待办解析、确认和创建主要由 Web 工作台继续处理。

### 2.3 Web 登录联动

桌面端没有登录信息时，会提示用户前往 Web 页面登录。

登录流程为：

```text
桌面端生成随机 state
→ 打开 Web 登录页
→ Web 登录成功
→ 通过 huali-ai-mascot://auth-callback 回调桌面端
→ 桌面端校验 state
→ 保存 token 和用户信息
→ 建立用户专属消息连接
```

登录信息保存在本地 `localStorage` 中。

### 2.4 实时消息提醒

项目包含两类 WebSocket 通道：

1. 任务事件通道

   接收 `task.created` 事件，用于提示新任务。

2. 站内消息通道

   连接地址格式为：

   ```text
   /websocket/{userId}
   ```

   接收 `sys_message` 消息，并在吉祥物上方展示通知卡片。

站内消息支持：

- 查看详情
- 标记为已读
- 多条消息排队展示
- 断线自动重连
- HTTP 轮询兜底
- 跳转 Web 日历详情页

### 2.5 系统托盘

托盘菜单支持：

- 打开工作台
- 显示助手
- 隐藏助手
- 退出登录
- 退出程序

### 2.6 Web 页面跳转

桌面端可以打开：

- Web 登录页
- 工作台
- 日历
- 站内消息详情
- Agents 页面

macOS 下会尝试复用 Chrome、Edge、Brave、Arc 或 Safari 中已打开的 Web 平台标签页。

## 3. 窗口结构

项目包含两个主要窗口：

| 窗口 | 作用 |
|---|---|
| `mascot` | 显示桌面吉祥物、登录提示和消息提醒 |
| `panel` | 显示一句话创建输入框 |

窗口均采用透明、无边框、置顶设计。输入面板会自动显示在吉祥物附近，并在吉祥物移动后同步位置。

## 4. 技术栈

| 技术 | 用途 |
|---|---|
| Tauri 2 | 桌面应用外壳及系统能力 |
| Rust | 窗口、托盘和浏览器联动 |
| Vue 3 | 前端界面 |
| TypeScript | 类型与业务逻辑 |
| Vite | 开发和构建 |
| Pinia | 用户、任务和吉祥物状态管理 |
| Axios | HTTP 接口请求 |
| WebSocket | 任务和站内消息推送 |
| Tauri Deep Link | Web 登录结果回调 |
| Tauri Opener | 打开外部 Web 页面 |

## 5. 项目目录说明

```text
DesktopMascot/
├── src/
│   ├── components/       # 吉祥物、输入框、登录提示、消息卡片
│   ├── views/            # 吉祥物窗口和操作面板
│   ├── services/         # 登录、WebSocket、接口和窗口服务
│   ├── stores/           # Pinia 状态管理
│   ├── types/            # TypeScript 类型
│   ├── utils/            # 环境变量、本地存储、时间工具
│   └── assets/           # 吉祥物图片和样式
├── src-tauri/
│   ├── src/main.rs       # 桌面窗口、托盘和系统能力
│   └── tauri.conf.json   # Tauri 应用配置
├── scripts/              # Mock 服务和素材处理脚本
├── downloads/            # 下载页面
└── package.json          # 依赖和运行命令
```

## 6. 当前实现状态

### 已接入当前界面的能力

- 桌面吉祥物展示与拖拽
- 动画状态切换
- 一句话输入并打开 Web 工作台
- Web 登录和 Deep Link 回调
- 用户登录信息持久化
- 站内消息接收与展示
- WebSocket 自动重连
- 托盘菜单
- Web 页面跳转

### 已有代码但尚未完整接入当前界面的能力

- 待办解析接口 `parseTodo`
- 任务操作接口
- 任务卡片组件
- 任务队列状态管理
- 模拟任务推送服务

### 当前限制

- 消息提醒只使用机器人上方的应用内卡片，不调用 Windows 通知中心。
- 语音按钮只切换“正在听”的界面状态，没有真正接入麦克风和语音识别。
- 输入内容目前直接交给 Web 工作台，没有在桌面端调用待办解析接口。
- 收到 `task.created` 后会打开面板，但当前面板只渲染输入框，没有渲染已有的任务卡片组件。
- 默认配置会启用 Mock 模式，正式环境需要设置 `VITE_ENABLE_MOCK=false`。

## 7. 项目目标总结

该项目的核心目标是：

> 将华力 Web 平台的待办创建、任务通知和站内消息能力延伸到桌面端，通过轻量吉祥物提供一个常驻、快捷、低打扰的办公入口。

它主要解决用户必须频繁打开浏览器检查任务的问题，让用户能够直接在桌面完成信息接收、快捷输入和业务页面跳转。
