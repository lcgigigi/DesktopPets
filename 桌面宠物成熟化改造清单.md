# 华力 AI 桌面宠物成熟化改造清单

> 目标：把当前“可演示、可试点”的桌面助手，逐步建设成安全、稳定、可升级、可长期运行的桌面宠物。
>
> 本文只列改造项，不代表所有项目都必须立即实施。评审时可在每项的“决策”栏填写：`做 / 暂缓 / 不做 / 需讨论`。

## 1. 使用说明

### 1.1 责任方标识

| 标识 | 含义 |
| --- | --- |
| `桌面端` | 当前 Tauri + Vue 桌面宠物项目需要修改 |
| `Web端` | Web 工作台、登录页、日历页等需要配合修改 |
| `后台` | API、WebSocket、鉴权、数据表或消息服务需要修改 |
| `运维` | HTTPS、证书、发布服务器、监控、企业分发等需要配合 |
| `产品/设计` | 需要确认产品行为、文案、交互或视觉规范 |

### 1.2 优先级

| 优先级 | 含义 |
| --- | --- |
| `P0` | 正式推广或处理真实账号数据前必须解决 |
| `P1` | 长期稳定运行前应解决 |
| `P2` | 提升使用体验、运营能力和宠物感 |

### 1.3 推荐评审字段

每项都可以在评审时补充：

- 决策：`待确认`
- 负责人：`待分配`
- 计划版本：`待确认`
- 备注：`无`

---

## 2. 阶段一：安全与接口契约

### SEC-01 登录改为一次性授权码换 Token

- 优先级：`P0`
- 责任方：`桌面端` `Web端` `后台`
- 当前问题：Web 登录成功后通过 Deep Link 查询参数直接返回 `token` 和用户信息，Token 可能进入浏览器历史、系统协议记录或日志。
- 桌面端修改：
  - Deep Link 只接收 `code` 和 `state`。
  - 收到回调后调用后台兑换接口获取 Token 和用户信息。
  - `state` 必须一次性使用，成功或失败后及时清理。
  - 为登录流程增加超时、取消和重复回调处理。
- Web端配合：
  - 登录成功后向后台申请一次性 `code`。
  - 回调 `huali-ai-mascot://auth-callback?code=...&state=...`。
  - 不再把 Token、用户名、部门等信息放入回调 URL。
- 后台配合：
  - 新增一次性授权码签发和兑换接口。
  - 授权码应绑定用户、客户端、`state`，短时有效且只能使用一次。
  - 兑换成功后立即作废授权码，并记录安全审计日志。
- 验收标准：浏览器地址栏、Deep Link 和系统 URL 记录中不出现真实 Token。
- 决策：`待确认`

### SEC-02 Token 使用系统安全存储

- 优先级：`P0`
- 责任方：`桌面端`
- 当前问题：Token 明文保存在 WebView 的 `localStorage` 中。
- 修改项：
  - 接入操作系统安全存储，例如 macOS Keychain、Windows Credential Manager 或 Tauri Stronghold。
  - `localStorage` 只允许保存非敏感偏好设置和必要的用户展示信息。
  - 升级时迁移并删除旧的本地 Token。
  - 退出登录时同时清理内存状态、安全存储和旧版遗留数据。
- 验收标准：开发者工具和 WebView 本地存储中无法读取真实 Token。
- 决策：`待确认`

### SEC-03 全链路启用 HTTPS/WSS

- 优先级：`P0`
- 责任方：`桌面端` `Web端` `后台` `运维`
- 当前问题：当前生产和 main 环境仍使用 HTTP/WS 地址。
- 桌面端修改：
  - 生产构建拒绝 `http://` 和 `ws://` 配置，localhost 开发环境除外。
  - API、Web 页面、任务 WebSocket、站内消息 WebSocket 全部使用安全协议。
- Web端配合：所有桌面联动页面通过 HTTPS 提供。
- 后台配合：API 和 WebSocket 支持 TLS 终止及安全连接。
- 运维配合：配置可信证书、证书续期、内网 DNS 和反向代理。
- 验收标准：生产安装包发起的业务请求全部为 HTTPS/WSS，证书异常时连接失败且给出可理解提示。
- 决策：`待确认`

### SEC-04 启用 CSP 并收紧 Tauri 权限

- 优先级：`P0`
- 责任方：`桌面端`
- 当前问题：Tauri 配置中的 CSP 为 `null`，窗口能力和 opener 权限需要进一步按最小权限收紧。
- 修改项：
  - 配置明确的 `default-src`、`script-src`、`style-src`、`img-src`、`connect-src`。
  - `connect-src` 只允许正式 API 和 WebSocket 域名。
  - 限制 opener 只能打开允许的 Web 域名和协议。
  - Rust 命令 `open_or_focus_web_url` 增加域名白名单，不能只判断是否为 HTTP URL。
  - 审核 mascot 和 panel 两个窗口的 capability，按窗口拆分不必要权限。
- 验收标准：恶意或错误 URL 无法通过 WebView 调用系统浏览器；正常 API、图片和 WebSocket 不受影响。
- 决策：`待确认`

### SEC-05 Token 生命周期、刷新和失效处理

- 优先级：`P0`
- 责任方：`桌面端` `Web端` `后台`
- 桌面端修改：
  - 明确 access token 和 refresh token 的使用方式。
  - API 返回 401 时进行一次受控刷新；刷新失败统一退出登录。
  - WebSocket 因鉴权失效断开时停止无意义重连，并提示重新登录。
  - 避免多个并发请求同时触发多次刷新。
- Web端配合：统一登录态过期和重新登录行为。
- 后台配合：提供刷新、撤销和登出接口；支持设备维度撤销 Token。
- 验收标准：Token 过期时不会无限重连、反复弹窗或进入半登录状态。
- 决策：`待确认`

### SEC-06 接口成功码和错误码统一

- 优先级：`P0`
- 责任方：`桌面端` `Web端` `后台`
- 当前问题：桌面端当前只把数字 `code === 200` 当成功，常见的 `code === 0` 会被误判为失败。
- 修改项：
  - 三端统一成功响应约定，例如 `{ code: 0, message, data }`。
  - 区分 HTTP 状态码和业务状态码。
  - 统一未登录、无权限、参数错误、重复操作、服务异常、版本过低等错误码。
  - 桌面端建立错误码到用户提示的映射，未知错误保留可追踪的 requestId。
- 验收标准：同一个接口在桌面端和 Web 端表现一致，成功响应不会被误判。
- 决策：`待确认`

### SEC-07 建立正式接口契约和版本机制

- 优先级：`P0`
- 责任方：`桌面端` `Web端` `后台`
- 修改项：
  - 使用 OpenAPI、JSON Schema 或明确的协议文档维护 API 和 WebSocket 数据结构。
  - 为字段注明必填、可选、枚举、最大长度、时间格式和兼容策略。
  - 为桌面接口增加版本，例如 `/api/desktop/v1/...`。
  - 新增字段必须向后兼容；删除或改名需要版本升级。
  - 桌面端收到未知字段时可忽略，收到缺失关键字段时安全失败。
- 验收标准：三端可根据同一份契约生成类型或执行契约测试。
- 决策：`待确认`

### SEC-08 启动时校验环境配置

- 优先级：`P0`
- 责任方：`桌面端` `运维`
- 当前问题：部分关键地址为空时程序只显示连接关闭，没有明确指出配置错误。
- 修改项：
  - 构建时校验 API、Web、任务 WS、消息 WS 地址。
  - 生产构建禁止 Mock、空地址、localhost 和不安全协议。
  - 启动后显示明确的环境与连接诊断，但不能展示 Token。
  - 区分 `development / test / main / production`，避免使用含义重复的环境文件。
- 验收标准：错误配置无法产出正式安装包，或启动后能够被立即发现。
- 决策：`待确认`

### SEC-09 输入校验、限流和重要操作保护

- 优先级：`P1`
- 责任方：`桌面端` `Web端` `后台`
- 修改项：
  - 限制待办文本、标题、备注等字段长度并清理异常字符。
  - 后台对创建、完成、取消、稍后提醒等接口进行权限校验和限流。
  - 所有修改状态的接口支持幂等键，避免重复点击或重试造成重复操作。
  - 重要操作保存操作者、设备、时间和结果审计记录。
- 验收标准：重复提交、越权操作和超长输入不会造成重复数据或服务异常。
- 决策：`待确认`

---

## 3. 阶段二：任务与消息可靠闭环

### FLOW-01 明确桌面端功能边界

- 优先级：`P0`
- 责任方：`产品/设计` `桌面端` `Web端` `后台`
- 需要确认：
  - 方案 A：桌面宠物只是 Web 工作台的快捷入口。
  - 方案 B：桌面端独立完成待办解析、确认、创建和任务处理。
- 影响：方案 A 应删除或隐藏未接入功能，避免用户误以为已在桌面完成创建；方案 B 需要完成下列全部业务闭环。
- 验收标准：产品文案、桌面行为和真实能力一致。
- 决策：`待确认`

### FLOW-02 接入任务卡片与任务队列

- 优先级：`P0`
- 责任方：`桌面端` `产品/设计`
- 当前问题：任务已经进入 Pinia Store，但 PanelWindow 没有渲染现有 TaskPushCard。
- 修改项：
  - PanelWindow 根据当前任务显示任务卡；无任务时显示输入框。
  - 支持多任务队列、上一条/下一条或未处理数量。
  - 明确完成、取消、稍后提醒、查看详情的交互结果。
  - 处理过程中禁用重复操作，失败时保留任务并允许重试。
- 验收标准：收到任务后用户能在桌面完成一次完整操作，且状态与后台一致。
- 决策：`待确认`

### FLOW-03 完成“一句话创建待办”闭环

- 优先级：`P0`（选择方案 B 时）
- 责任方：`桌面端` `Web端` `后台` `产品/设计`
- 桌面端修改：
  - 调用解析接口，而不是只把原文带到 Web 工作台。
  - 展示解析结果并允许确认、修改、取消。
  - 创建成功后显示结果；失败时保留原输入并允许重试。
- Web端配合：
  - 保留从桌面打开草稿详情或继续编辑的入口。
  - 正确识别 `todoDraftId`，避免重复创建。
- 后台配合：
  - 提供解析、草稿详情、从草稿创建、草稿过期等接口。
  - 创建接口支持幂等，返回最终待办 ID。
- 验收标准：输入一句话后能够确认并生成唯一待办，桌面和 Web 看到同一条数据。
- 决策：`待确认`

### FLOW-04 补齐任务操作后台接口

- 优先级：`P0`
- 责任方：`桌面端` `后台`
- 当前问题：“稍后提醒”目前只在桌面端返回本地成功。
- 修改项：
  - 后台提供完成、取消、稍后提醒和查看详情所需接口。
  - “稍后提醒”必须保存下次提醒时间和次数。
  - 桌面端只有收到后台成功响应后才能移除任务。
  - 接口返回当前任务版本和最终状态，处理多人同时操作冲突。
- 验收标准：重启桌面端后任务状态仍正确，稍后提醒能在约定时间再次出现。
- 决策：`待确认`

### FLOW-05 补齐站内消息已读闭环

- 优先级：`P0`
- 责任方：`桌面端` `Web端` `后台`
- 当前问题：桌面端点击已读目前只隐藏本地消息。
- 修改项：
  - 后台提供单条已读、批量已读和未读列表接口。
  - 桌面端成功上报已读后再更新本地状态；失败时允许重试。
  - Web 端和桌面端的未读数量保持一致。
- 验收标准：任一端标记已读后，其他端刷新或收到同步事件后状态一致。
- 决策：`待确认`

### FLOW-06 WebSocket 统一鉴权

- 优先级：`P0`
- 责任方：`桌面端` `后台`
- 当前问题：任务通道和站内消息通道实现分离，消息通道主要依赖 URL 中的 userId。
- 修改项：
  - 连接时使用短期连接票据或受控鉴权消息，不在 URL 中放长期 Token。
  - 后台根据 Token 确定用户，不能信任客户端传入的 userId。
  - 登录用户变化时关闭旧连接并清空旧用户消息。
  - 鉴权失败使用明确关闭码，桌面端据此决定刷新或退出登录。
- 验收标准：修改 URL 中的 userId 无法订阅其他用户消息。
- 决策：`待确认`

### FLOW-07 统一任务和消息事件协议

- 优先级：`P0`
- 责任方：`桌面端` `Web端` `后台`
- 建议事件公共字段：
  - `eventId`
  - `eventType`
  - `eventVersion`
  - `timestamp`
  - `userId`
  - `deviceId` 或广播范围
  - `needAck`
  - `payload`
- 修改项：
  - 为任务创建、任务更新、任务取消、消息新增、消息已读、强制升级等定义事件类型。
  - 后台保证 eventId 全局唯一。
  - 桌面端在运行时验证关键字段，非法消息写入脱敏日志并丢弃。
- 验收标准：协议有正式文档和示例，三端使用相同字段含义。
- 决策：`待确认`

### FLOW-08 心跳、超时和连接状态机

- 优先级：`P0`
- 责任方：`桌面端` `后台`
- 修改项：
  - 后台定期发送 ping 或要求客户端心跳。
  - 桌面端检测“连接存在但实际已失效”的半开连接。
  - 明确 connecting、authenticated、connected、reconnecting、offline、auth-expired 状态。
  - 连接超时后主动关闭并重连。
- 验收标准：拔网线、切换网络、服务端重启后，连接状态能准确变化并自动恢复。
- 决策：`待确认`

### FLOW-09 ACK、去重、重放和补拉

- 优先级：`P0`
- 责任方：`桌面端` `后台`
- 修改项：
  - 对 `needAck` 事件发送 ACK。
  - 桌面端持久化最近处理过的 eventId，避免重复弹窗和重复操作。
  - 后台保存未确认事件，并在重连后重放。
  - 桌面端带上最后成功处理的游标或时间戳进行断点续传。
  - 超出重放窗口时通过未处理任务/未读消息接口补拉。
- 验收标准：断网期间产生的任务在恢复后不会丢失，也不会重复处理。
- 决策：`待确认`

### FLOW-10 改进重连策略

- 优先级：`P1`
- 责任方：`桌面端` `后台`
- 修改项：
  - 使用指数退避和随机抖动，设置最大间隔。
  - 浏览器/系统报告离线时暂停重连，恢复在线后立即尝试。
  - 鉴权失败、版本过低等不可恢复错误不得无限重连。
  - 后台控制单用户/单设备连接数量，防止重连风暴。
- 验收标准：服务端大面积重启时不会出现所有客户端同时高频重连。
- 决策：`待确认`

### FLOW-11 处理系统休眠、唤醒和网络切换

- 优先级：`P1`
- 责任方：`桌面端`
- 修改项：
  - 监听系统休眠、唤醒、网络在线/离线事件。
  - 唤醒后重新验证 Token、重建连接并执行补拉。
  - 系统时间发生大幅变化时重新计算稍后提醒和定时任务。
- 验收标准：电脑休眠一夜后唤醒，用户无需重启应用即可继续收到消息。
- 决策：`待确认`

### FLOW-12 本地持久化任务和消息状态

- 优先级：`P1`
- 责任方：`桌面端`
- 修改项：
  - 保存待处理任务、未确认事件、最近 eventId 和重连游标。
  - 数据按用户隔离，切换用户时不能看到上一用户内容。
  - 设置最大容量和过期清理规则。
  - 敏感正文按数据安全要求决定是否落盘或加密。
- 验收标准：应用异常退出或升级后，待处理状态不会无故丢失或串用户。
- 决策：`待确认`

### FLOW-13 使用原生系统通知

- 优先级：`P1`
- 责任方：`桌面端` `产品/设计`
- 当前问题：通知插件已安装，但业务未真正调用系统通知。
- 修改项：
  - 请求并记录系统通知权限。
  - 宠物隐藏、屏幕锁定或窗口不活跃时发送系统通知。
  - 点击通知打开对应任务或消息详情。
  - 遵守免打扰、声音和隐私预览设置。
- 验收标准：隐藏宠物后仍可收到重要提醒，点击后能准确打开对应内容。
- 决策：`待确认`

### FLOW-14 设备注册和在线状态

- 优先级：`P1`
- 责任方：`桌面端` `后台`
- 修改项：
  - 首次登录注册设备 ID、系统、应用版本和推送能力。
  - 定期更新最后在线时间。
  - 支持后台撤销某台设备、查看活跃设备和限制过旧客户端。
  - 设备 ID 不能直接使用容易变化或侵犯隐私的硬件标识。
- 验收标准：后台可以定位某版本、某设备的连接和消息投递情况。
- 决策：`待确认`

### FLOW-15 单实例与 Deep Link 可靠转发

- 优先级：`P1`
- 责任方：`桌面端`
- 修改项：
  - 第二次启动时将 Deep Link 参数可靠转发给已运行实例。
  - 重复登录回调不能创建多个连接或覆盖新会话。
  - 非法、过期和重复回调有明确日志，但不记录敏感参数。
- 验收标准：应用运行期间再次触发登录协议，已有实例能够稳定处理。
- 决策：`待确认`

---

## 4. 阶段三：测试、日志与可观测性

### QA-01 增加前端单元测试

- 优先级：`P0`
- 责任方：`桌面端`
- 修改项：
  - 引入 Vitest。
  - 覆盖响应拦截器成功码和错误码。
  - 覆盖 Deep Link 解析、state 校验、重复回调和过期回调。
  - 覆盖 URL 构造、环境校验、消息标准化、任务去重和重连状态机。
  - 时间、随机数、网络和 WebSocket 使用可控 Mock。
- 验收标准：核心服务层具备稳定自动化测试，修复问题时必须补回归用例。
- 决策：`待确认`

### QA-02 增加组件和 Store 测试

- 优先级：`P1`
- 责任方：`桌面端`
- 修改项：
  - 测试任务卡操作、错误重试、处理中防重复点击。
  - 测试消息队列顺序、去重和已读处理。
  - 测试登录/退出后状态清理。
  - 测试输入框长度、空白输入和异常恢复。
- 验收标准：主要用户操作不依赖人工点击才能发现回归。
- 决策：`待确认`

### QA-03 增加 Rust 单元测试和静态检查

- 优先级：`P1`
- 责任方：`桌面端`
- 修改项：
  - 为 URL 白名单、窗口位置计算、多显示器边界计算增加 Rust 测试。
  - CI 执行 `cargo fmt --check`、`cargo clippy -D warnings` 和 `cargo test`。
  - 对关键 Tauri 命令返回真实错误，避免全部静默忽略。
- 验收标准：窗口和安全逻辑修改后可自动验证。
- 决策：`待确认`

### QA-04 建立 API 和 WebSocket 契约测试

- 优先级：`P0`
- 责任方：`桌面端` `Web端` `后台`
- 修改项：
  - 后台在 CI 中验证 OpenAPI/Schema。
  - 桌面和 Web 使用固定协议样例执行兼容性测试。
  - 对新增、缺失、未知字段和旧客户端版本进行兼容测试。
- 验收标准：后台协议变更在合并前就能发现是否破坏桌面端。
- 决策：`待确认`

### QA-05 建立桌面端端到端测试

- 优先级：`P1`
- 责任方：`桌面端` `后台`
- 修改项：
  - 覆盖启动、登录回调、双窗口、托盘、任务推送、消息已读和退出。
  - 使用可控测试服务器模拟断线、延迟、乱序、重复事件和鉴权过期。
  - Windows 和 macOS 至少各执行一套冒烟测试。
- 验收标准：正式打包前能自动完成核心业务冒烟验证。
- 决策：`待确认`

### QA-06 建立可靠性场景测试清单

- 优先级：`P1`
- 责任方：`桌面端` `后台` `运维`
- 必测场景：
  - 断网、弱网、切换 Wi-Fi、有线/无线切换。
  - 后台重启、WebSocket 网关重启、返回 500 或超时。
  - 电脑休眠/唤醒、锁屏/解锁、系统时间变化。
  - 多显示器插拔、不同缩放比例、主显示器切换。
  - Token 过期、用户被禁用、在其他设备退出。
  - 应用异常退出、强制结束、升级中断、磁盘空间不足。
- 验收标准：每个场景都有预期结果、测试记录和失败处理方式。
- 决策：`待确认`

### QA-07 长时间运行和性能基线

- 优先级：`P1`
- 责任方：`桌面端`
- 修改项：
  - 执行 24、72 小时持续运行测试。
  - 记录空闲、动画、消息突发时的 CPU、内存和句柄数量。
  - 检查 WebSocket、事件监听器、定时器和图片资源是否泄漏。
  - 设定可接受的 CPU/内存目标和回归阈值。
- 验收标准：持续运行期间内存不会无界增长，空闲 CPU 保持在约定范围。
- 决策：`待确认`

### QA-08 增加代码质量工具

- 优先级：`P1`
- 责任方：`桌面端`
- 修改项：
  - 引入 ESLint、Prettier 和必要的 Vue/TypeScript 规则。
  - 增加 `lint`、`format:check`、`test`、`check` 脚本。
  - 可选增加 pre-commit 检查，但不能明显拖慢日常开发。
- 验收标准：本地和 CI 使用同一套质量命令。
- 决策：`待确认`

### QA-09 建立持续集成

- 优先级：`P0`
- 责任方：`桌面端` `运维`
- 修改项：
  - 每次提交执行类型检查、Lint、单元测试、Rust 检查和前端构建。
  - 主分支执行 Windows/macOS 安装包构建或定期构建。
  - 缓存 npm 和 Cargo 依赖，同时保证锁文件参与构建。
  - 构建失败时禁止发布。
- 验收标准：任何正式安装包都能追溯到通过 CI 的确定提交。
- 决策：`待确认`

### LOG-01 桌面端结构化滚动日志

- 优先级：`P0`
- 责任方：`桌面端`
- 修改项：
  - 接入 Tauri 日志能力，统一前端和 Rust 日志。
  - 按日期或大小滚动，限制保留天数和总容量。
  - 记录启动、版本、环境、登录状态变化、连接状态、事件 ID、操作结果和错误码。
  - 禁止记录 Token、授权码、完整 URL 查询参数和敏感业务正文。
  - 每次启动生成 sessionId，便于串联一次运行过程。
- 验收标准：用户反馈“收不到消息”时，可以通过日志判断是鉴权、网络、协议还是后台投递问题。
- 决策：`待确认`

### LOG-02 后台链路追踪与审计

- 优先级：`P0`
- 责任方：`后台` `运维`
- 修改项：
  - API 响应返回 requestId/traceId。
  - 消息投递记录 eventId、目标用户/设备、发送、ACK、重试和最终状态。
  - 重要任务操作保留审计日志。
  - 桌面日志中的 eventId/requestId 能与后台日志关联。
- 验收标准：单条任务从后台产生到桌面处理的全链路可以追踪。
- 决策：`待确认`

### LOG-03 诊断信息导出

- 优先级：`P1`
- 责任方：`桌面端` `运维`
- 修改项：
  - 托盘或设置页提供“一键导出诊断信息”。
  - 包含应用版本、系统版本、连接状态、最近脱敏日志和配置完整性。
  - 导出前自动脱敏，允许用户预览保存位置。
- 验收标准：普通用户无需打开开发者工具即可提供可用排障资料。
- 决策：`待确认`

### LOG-04 监控、指标和告警

- 优先级：`P1`
- 责任方：`后台` `运维`
- 修改项：
  - 监控在线连接数、鉴权失败率、重连率、消息积压、ACK 延迟和投递失败率。
  - 监控登录码兑换失败、API 错误率和客户端版本分布。
  - 为异常峰值配置告警和处理手册。
- 验收标准：大面积收不到消息时，运维能在用户集中反馈前发现。
- 决策：`待确认`

---

## 5. 阶段四：自动更新、签名与发布治理

### REL-01 统一版本号来源

- 优先级：`P0`
- 责任方：`桌面端`
- 当前问题：package.json、Cargo.toml 和 tauri.conf.json 都有版本号，可能不一致。
- 修改项：
  - 确定单一版本来源并在构建前同步校验。
  - 使用语义化版本，例如 `1.2.3`。
  - 安装包文件名、关于页、日志和后台设备信息使用同一版本。
- 验收标准：任一发布物中的版本信息完全一致。
- 决策：`待确认`

### REL-02 接入 Tauri Updater

- 优先级：`P1`
- 责任方：`桌面端` `后台` `运维`
- 桌面端修改：
  - 接入更新检查、下载、签名验证、安装和重启流程。
  - 支持启动检查、手动检查和后台静默检查。
  - 下载或安装失败时保持当前版本可用。
- 后台/运维配合：
  - 提供更新清单和安装包下载地址。
  - 按平台、架构、渠道和版本返回正确更新。
  - 支持 Range 请求、稳定下载和访问日志。
- 验收标准：旧版本可升级到新版本，断网或下载中断不会损坏现有安装。
- 决策：`待确认`

### REL-03 更新包签名验证

- 优先级：`P0`（启用自动更新时）
- 责任方：`桌面端` `运维`
- 修改项：
  - 生成并安全保管 updater 私钥，公钥内置于客户端。
  - CI 使用受保护的密钥签署更新包。
  - 客户端拒绝未签名、签名错误或版本信息不匹配的包。
- 验收标准：替换更新包后客户端能够识别并拒绝安装。
- 决策：`待确认`

### REL-04 Windows 代码签名

- 优先级：`P1`
- 责任方：`桌面端` `运维`
- 修改项：
  - 申请企业代码签名证书。
  - 对 EXE、NSIS 安装包和必要的二进制进行签名和时间戳。
  - 在真实企业终端和安全软件环境测试安装、升级和卸载。
- 验收标准：安装包签名可验证，发布者信息正确，安全软件误报显著降低。
- 决策：`待确认`

### REL-05 macOS 签名与公证

- 优先级：`P1`（正式支持 macOS 时）
- 责任方：`桌面端` `运维`
- 修改项：
  - 使用 Developer ID 签名。
  - 配置 Hardened Runtime、权限声明和 Apple 公证。
  - 对最终 DMG/App 执行 Gatekeeper 验证。
- 验收标准：用户无需绕过系统安全提示即可正常安装运行。
- 决策：`待确认`

### REL-06 建立发布渠道和灰度策略

- 优先级：`P1`
- 责任方：`桌面端` `后台` `运维` `产品/设计`
- 修改项：
  - 至少区分 `test / main / stable` 渠道。
  - 支持按部门、用户、设备或百分比灰度。
  - 后台记录每个版本的发布时间、适用范围和已知问题。
  - 客户端设置中显示当前渠道，普通用户不可随意切换到测试渠道。
- 验收标准：新版本可以先给少量用户验证，再逐步扩大范围。
- 决策：`待确认`

### REL-07 回滚、最低版本和强制升级

- 优先级：`P1`
- 责任方：`桌面端` `后台` `运维` `产品/设计`
- 修改项：
  - 更新服务可以停止分发有问题的版本并恢复上一个稳定版本。
  - 后台返回最低支持版本和建议版本。
  - 强制升级只用于安全或协议不兼容场景，并提供明确说明。
  - 升级失败时仍能进入诊断或重新下载流程。
- 验收标准：发现严重问题后能快速停止扩散，旧客户端不会无限使用不兼容接口。
- 决策：`待确认`

### REL-08 发布产物、哈希和更新日志

- 优先级：`P0`
- 责任方：`桌面端` `运维`
- 修改项：
  - 每个版本保存安装包、签名、SHA-256、提交号和构建日志。
  - 自动生成或维护用户可读更新日志。
  - 发布页清楚区分安装版、便携版、平台和环境。
  - 安装包只能由 CI 或受控发布机生成。
- 验收标准：任何用户安装包都能追溯来源并验证完整性。
- 决策：`待确认`

### REL-09 依赖安全和供应链检查

- 优先级：`P1`
- 责任方：`桌面端` `运维`
- 修改项：
  - CI 执行 npm 和 Cargo 依赖漏洞检查。
  - 定期升级 Tauri、Vue、Axios 等依赖，并保留回归测试。
  - 锁定依赖和构建工具版本。
  - 可选生成 SBOM，记录安装包包含的依赖。
  - 签名密钥、证书和发布凭据只能放在受保护的 CI Secret 中。
- 验收标准：高危依赖漏洞有明确处理时限，发布密钥不进入仓库或构建日志。
- 决策：`待确认`

### REL-10 企业分发与卸载策略

- 优先级：`P1`
- 责任方：`桌面端` `运维`
- 修改项：
  - 明确内网下载、终端管理平台、域控等分发方式。
  - 支持静默安装、升级和卸载时，需要定义参数和权限范围。
  - 卸载时明确是否保留偏好设置、日志和安全存储凭据。
  - 提供安装失败和安全软件拦截处理说明。
- 验收标准：运维能够批量部署并知道如何安全回收客户端。
- 决策：`待确认`

---

## 6. 阶段五：设置与桌面宠物体验

### UX-01 新增设置窗口

- 优先级：`P1`
- 责任方：`桌面端` `产品/设计`
- 建议设置：
  - 开机自启。
  - 始终置顶。
  - 显示/隐藏宠物。
  - 通知、声音和消息预览。
  - 免打扰时间。
  - 动画频率和低性能模式。
  - 透明度、大小或缩放。
  - 登录账号和退出登录。
  - 当前版本、更新渠道和检查更新。
  - 导出诊断信息。
- 验收标准：用户无需修改配置文件即可控制主要行为。
- 决策：`待确认`

### UX-02 可配置开机自启

- 优先级：`P1`
- 责任方：`桌面端` `产品/设计`
- 修改项：
  - 接入系统自启动能力，默认值由产品确认。
  - 用户可以随时关闭。
  - 自启动时避免抢占焦点和立即弹出非重要内容。
- 验收标准：启用后登录系统自动运行，关闭后不再启动，状态与系统设置一致。
- 决策：`待确认`

### UX-03 保存窗口位置并处理多显示器

- 优先级：`P1`
- 责任方：`桌面端`
- 当前问题：每次显示助手都会重新放到右下角，拖动位置未持久化且没有完整屏幕边界限制。
- 修改项：
  - 保存宠物最后位置、显示器和缩放信息。
  - 拖动时限制可见边界，至少保留可拖回区域。
  - 显示器拔出、分辨率或缩放变化时自动迁移到可见区域。
  - 面板和气泡始终优先显示在当前屏幕内部。
- 验收标准：重启后位置合理，多显示器变化后宠物不会消失在屏幕外。
- 决策：`待确认`

### UX-04 点击穿透、快速隐藏和遮挡控制

- 优先级：`P2`
- 责任方：`桌面端` `产品/设计`
- 修改项：
  - 提供临时点击穿透或工作模式。
  - 提供全局快捷键或托盘操作快速显示/隐藏。
  - 全屏演示、游戏或会议共享时可自动隐藏，具体行为由用户设置。
  - 不应在输入、截图或关键操作时频繁抢焦点。
- 验收标准：宠物不会持续遮挡业务软件，也不会造成无法点击的区域。
- 决策：`待确认`

### UX-05 免打扰和提醒分级

- 优先级：`P1`
- 责任方：`桌面端` `后台` `产品/设计`
- 修改项：
  - 消息定义普通、重要、紧急等级。
  - 桌面端支持免打扰时间、仅重要通知和静音。
  - 后台不能把所有消息都标记为紧急；紧急消息需要受控权限。
  - 免打扰期间的普通消息进入未读队列，结束后汇总提示。
- 验收标准：长期运行不会因为高频弹窗干扰工作，重要消息仍可到达。
- 决策：`待确认`

### UX-06 建立宠物动画状态机

- 优先级：`P2`
- 责任方：`桌面端` `产品/设计`
- 当前问题：当前主要在两套精灵图间切换，多种业务状态共享相同动作。
- 修改项：
  - 定义 idle、hover、working、waiting、remind、success、error、sleep 等状态及优先级。
  - 区分循环动画、一次性动画和可被打断动画。
  - 处理通知、拖动、点击、网络变化同时发生时的状态冲突。
  - 动画结束后必须回到正确业务状态，而不是统一回到 idle。
- 验收标准：连续触发多种事件时动画不会卡死、闪烁或覆盖重要提醒。
- 决策：`待确认`

### UX-07 增加克制的待机互动和人格反馈

- 优先级：`P2`
- 责任方：`桌面端` `产品/设计`
- 修改项：
  - 增加低频随机待机动作、问候和工作状态反馈。
  - 设置每日次数和最短间隔，禁止连续打扰。
  - 文案与真实状态一致，不能在操作失败时给出成功暗示。
  - 支持关闭主动互动。
- 验收标准：宠物有生命感，但不会成为持续弹窗工具。
- 决策：`待确认`

### UX-08 主题、皮肤和动作资源扩展机制

- 优先级：`P2`
- 责任方：`桌面端` `产品/设计`
- 修改项：
  - 定义精灵图尺寸、帧数、帧率、透明背景和命名规范。
  - 资源与状态配置解耦，新增动作不需要修改大量业务代码。
  - 支持资源版本和回退，损坏或缺失时使用默认动作。
  - 控制资源包大小，避免更新包持续膨胀。
- 验收标准：新增皮肤或动作可以通过标准资源包完成，旧版本有安全回退。
- 决策：`待确认`

### UX-09 空闲性能和低性能模式

- 优先级：`P1`
- 责任方：`桌面端`
- 修改项：
  - 窗口隐藏、系统锁屏或设备低电量时暂停非必要动画和定时器。
  - 降低空闲状态帧率。
  - 提供关闭阴影、模糊和复杂动画的低性能模式。
  - 图片按实际显示尺寸压缩，避免加载无用大图。
- 验收标准：长期空闲时 CPU 占用稳定，低配办公电脑不明显卡顿。
- 决策：`待确认`

### UX-10 无障碍与键盘操作

- 优先级：`P2`
- 责任方：`桌面端` `产品/设计`
- 修改项：
  - 所有任务操作可通过键盘完成并具备可见焦点。
  - 按钮和输入框有准确语义、标签和禁用说明。
  - 支持系统减少动态效果偏好。
  - 颜色和状态不能只靠颜色区分。
- 验收标准：只使用键盘也能完成登录、创建、处理任务和关闭提示。
- 决策：`待确认`

### UX-11 隐私展示设置

- 优先级：`P1`
- 责任方：`桌面端` `后台` `产品/设计`
- 修改项：
  - 支持锁屏或共享屏幕时隐藏任务正文和人员信息。
  - 系统通知可选择仅显示“收到新消息”。
  - 后台为敏感消息提供不可预览标识。
- 验收标准：桌面通知和宠物气泡不会在不合适场景泄露敏感业务内容。
- 决策：`待确认`

---

## 7. 跨阶段工程与项目管理项

### PM-01 清理并形成可复现的 Git 基线

- 优先级：`P0`
- 责任方：`桌面端`
- 当前问题：当前仓库只有初始提交，工作区存在大量未提交修改。
- 修改项：
  - 按功能整理提交，避免所有改动长期堆积在工作区。
  - 为正式发布创建版本标签。
  - 保证 `.env` 不包含密钥；任何 `VITE_` 变量都应视为可被客户端读取。
  - 清理无用资源和未接入代码，或明确标记为后续功能。
- 验收标准：从版本标签重新拉取仓库后能够构建出同版本安装包。
- 决策：`待确认`

### PM-02 建立问题分级和发布门槛

- 优先级：`P1`
- 责任方：`桌面端` `Web端` `后台` `运维` `产品/设计`
- 修改项：
  - 定义阻断、严重、一般、体验问题等级。
  - 明确 P0/P1 问题是否允许带入正式版本。
  - 发布前由桌面、Web、后台和运维分别确认负责项。
  - 建立已知问题列表和回滚负责人。
- 验收标准：发布决定有明确依据和负责人，不依赖口头确认。
- 决策：`待确认`

### PM-03 建立兼容支持矩阵

- 优先级：`P1`
- 责任方：`桌面端` `Web端` `后台` `运维`
- 修改项：
  - 明确支持的 Windows、macOS、CPU 架构和 WebView2 版本。
  - 明确后台协议支持的最旧桌面版本。
  - 明确不再支持版本的提示和升级路径。
- 验收标准：测试范围、安装说明和后台兼容策略使用同一份支持矩阵。
- 决策：`待确认`

---

## 8. 责任方汇总

### 8.1 明确需要 Web 端配合的项目

- `SEC-01` 登录页签发授权码并通过 Deep Link 回传。
- `SEC-03` Web 联动页面启用 HTTPS。
- `SEC-05` 统一登录过期和重新登录体验。
- `SEC-06` 统一响应码和错误码。
- `SEC-07` 共用正式接口契约。
- `SEC-09` 输入校验和重要操作保护。
- `FLOW-01` 明确桌面与 Web 的能力边界。
- `FLOW-03` 支持 todoDraftId、草稿继续编辑和防重复创建。
- `FLOW-05` 同步站内消息已读状态。
- `FLOW-07` 使用统一事件协议。
- `QA-04` 参与 API/WebSocket 契约测试。
- `PM-02` 参与联合发布确认。
- `PM-03` 参与兼容支持矩阵确认。

### 8.2 明确需要后台配合的项目

- `SEC-01` 一次性授权码签发与兑换。
- `SEC-03` API 和 WebSocket TLS 支持。
- `SEC-05` Token 刷新、撤销和设备登出。
- `SEC-06` 统一响应码和错误码。
- `SEC-07` API/WebSocket 契约和版本管理。
- `SEC-09` 权限、限流、幂等和审计。
- `FLOW-01` 明确业务边界。
- `FLOW-03` 解析、草稿和创建接口。
- `FLOW-04` 完成、取消、稍后提醒及状态冲突处理。
- `FLOW-05` 已读、未读列表和多端同步。
- `FLOW-06` WebSocket 鉴权。
- `FLOW-07` 统一事件协议。
- `FLOW-08` 心跳和连接状态。
- `FLOW-09` ACK、重放、游标和补拉。
- `FLOW-10` 连接限流和重连风暴保护。
- `FLOW-14` 设备注册、在线状态和设备撤销。
- `QA-04` 契约测试。
- `QA-05` 提供可控测试环境。
- `QA-06` 配合故障场景测试。
- `LOG-02` 链路追踪与审计。
- `LOG-04` 指标和告警。
- `REL-02` 提供更新服务或更新元数据能力。
- `REL-06` 灰度范围和版本分发策略。
- `REL-07` 最低版本和强制升级策略。
- `UX-05` 消息优先级。
- `UX-11` 敏感消息不可预览标识。
- `PM-02` 联合发布确认。
- `PM-03` 后台协议兼容范围。

### 8.3 明确需要运维配合的项目

- `SEC-03` TLS 证书、DNS 和反向代理。
- `SEC-08` 环境配置和正式构建约束。
- `QA-06` 故障演练。
- `QA-09` CI 环境。
- `LOG-02` 日志平台。
- `LOG-03` 诊断资料处理流程。
- `LOG-04` 监控与告警。
- `REL-02` 更新文件托管。
- `REL-03` 更新签名密钥保管。
- `REL-04` Windows 证书。
- `REL-05` macOS 证书和公证凭据。
- `REL-06` 灰度发布。
- `REL-07` 回滚和强制升级。
- `REL-08` 发布产物归档。
- `REL-09` 供应链安全与 CI Secret。
- `REL-10` 企业安装和卸载。
- `PM-02` 发布确认。
- `PM-03` 系统支持范围。

---

## 9. 推荐实施顺序与阶段门槛

### 第一批：正式使用前的 P0 基线

建议至少完成：

1. `SEC-01` 至 `SEC-08`。
2. `FLOW-01` 产品边界确认。
3. `FLOW-02` 至 `FLOW-09` 中与最终产品边界相关的项目。
4. `QA-01`、`QA-04`、`QA-09`。
5. `LOG-01`、`LOG-02`。
6. `REL-01`、`REL-08`。
7. `PM-01`。

阶段门槛：真实 Token 不通过 URL 或 localStorage 保存；消息不因短时断网丢失；正式安装包可追溯。

### 第二批：长期稳定运行

建议完成：

1. `FLOW-10` 至 `FLOW-15`。
2. `QA-02`、`QA-03`、`QA-05` 至 `QA-08`。
3. `LOG-03`、`LOG-04`。
4. `REL-02` 至 `REL-07`、`REL-09`、`REL-10`。
5. `UX-01` 至 `UX-03`、`UX-05`、`UX-09`、`UX-11`。
6. `PM-02`、`PM-03`。

阶段门槛：应用可连续运行 72 小时；休眠、断网和后台重启后能自行恢复；能够灰度升级和快速止损。

### 第三批：完善桌面宠物体验

建议完成：

1. `UX-04`、`UX-06` 至 `UX-08`、`UX-10`。
2. 根据用户反馈继续扩展动作、主题和互动，但必须受免打扰和性能约束。

阶段门槛：宠物有明确状态和人格反馈，不遮挡工作、不高频打扰，并能在低配办公电脑上稳定运行。

---

## 10. 最终验收总表

| 验收领域 | 最低通过条件 | 结果 |
| --- | --- | --- |
| 登录安全 | Token 不出现在 URL/localStorage，支持过期和撤销 | `待验收` |
| 传输安全 | 正式环境全部使用 HTTPS/WSS | `待验收` |
| 接口契约 | 成功码、错误码、字段和版本规则统一 | `待验收` |
| 消息可靠性 | 支持鉴权、心跳、ACK、去重、重放和补拉 | `待验收` |
| 业务闭环 | 创建、任务操作、稍后提醒、已读状态与后台一致 | `待验收` |
| 自动化测试 | 核心逻辑、契约和桌面冒烟测试进入 CI | `待验收` |
| 可观测性 | 桌面日志可与后台 eventId/requestId 关联 | `待验收` |
| 发布治理 | 安装包可追溯、可验证、可灰度、可停止分发 | `待验收` |
| 更新安全 | 更新包签名校验，失败不破坏现有版本 | `待验收` |
| 长期运行 | 72 小时无明显泄漏，休眠/断网后可恢复 | `待验收` |
| 用户控制 | 可控制自启、通知、免打扰、置顶和动画 | `待验收` |
| 宠物体验 | 状态清晰、不过度打扰、不遮挡工作 | `待验收` |

